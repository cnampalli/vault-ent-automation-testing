[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -q"
markers = [
    "area(name): functional area this test belongs to (used for the console summary)",
]
